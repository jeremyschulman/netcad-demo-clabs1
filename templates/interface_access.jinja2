{% import "macros/macro_stp.jinja2" as stp %}
interface {{ interface.name }}
   description {{ interface.desc }}
   switchport access vlan {{ interface.profile.vlan.vlan_id }}
   {{ stp.stp_access() | indent(3) }}
!
